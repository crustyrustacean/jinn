//! Shared protocol types — re-exported from `nullslop-protocol`.
//!
//! This module re-exports everything from `nullslop-protocol` so that downstream
//! crates can depend on `nullslop-domain` alone for both protocol types and domain logic.

pub use nullslop_protocol::*;
